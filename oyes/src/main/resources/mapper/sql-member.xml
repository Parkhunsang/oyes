<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.oyes.mapper.MemberMapper">
	<select id="select_no" parameterType="integer" resultType="member">
		select * from member where patient_no = #{patient_no}
	</select>
	
	<select id="select" parameterType="string" resultType="member">
		select * from member where patient_id = #{patient_id}
	</select>
	
	<insert id="insert" parameterType="member">
		<selectKey keyProperty="patient_no" order="BEFORE" resultType="integer">
			select nvl(max(patient_no),0) + 1 from member
		</selectKey>
		insert into member values 
		(#{patient_no}, #{patient_id}, #{password}, #{patient_name}, #{birth_date}, #{mobile_no}, #{email}, 'n', sysdate, sysdate, 'y')
	</insert>
	
	<update id="update" parameterType="member">
		update member 
		set    password     = #{password}, 
		       patient_name = #{patient_name}, 
		       birth_date   = #{birth_date}, 
		       mobile_no    = #{mobile_no}, 
		       email        = #{email},
		       update_date  = sysdate
		where patient_id = #{patient_id}
	</update>
	
	<update id="delete" parameterType="string">
		update member 
		set    is_active = 'n' 
		where patient_id = #{patient_id}
	</update>
</mapper>
